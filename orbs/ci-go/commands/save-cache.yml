description: "Save the go mod & build cache."
steps:
  - save_cache:
      key: v2-gosrc-{{ .Branch }}-{{ checksum "go.sum" }}-{{ .Environment.CIRCLE_JOB }}
      paths:
        - /go/pkg/mod
  - save_cache:
      key: v2-gobin-{{ arch }}-{{ .Branch }}-{{ checksum "go.sum" }}-{{ .Environment.CIRCLE_JOB }}
      paths:
        - /go/cache
